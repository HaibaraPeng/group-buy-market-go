server:
  port: 8080
  host: localhost

database:
  host: localhost
  port: 3306
  username: root
  password: root
  dbname: group_buy_market

redis:
  host: localhost
  port: 6379

logging:
  level: info
  format: json