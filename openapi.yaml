# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: ""
    version: 0.0.1
paths:
    /dcc/updateConfig:
        post:
            tags:
                - DccHTTP
            description: UpdateConfig
            operationId: DccHTTP_UpdateConfig
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateConfigRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UpdateConfigReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /market/trial:
        post:
            tags:
                - ActivityHTTP
            description: MarketTrial handles market trial requests
            operationId: ActivityHTTP_MarketTrial
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/MarketTrialRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MarketTrialReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /tag/execTagBatchJob:
        post:
            tags:
                - TagHTTP
            description: ExecTagBatchJob
            operationId: TagHTTP_ExecTagBatchJob
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ExecTagBatchJobRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ExecTagBatchJobReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /trade/lockMarketPayOrder:
        post:
            tags:
                - TradeHTTP
            description: QueryNoPayMarketPayOrderByOutTradeNo 查询未支付的营销订单
            operationId: TradeHTTP_LockMarketPayOrder
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/LockMarketPayOrderRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/LockMarketPayOrderReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        ExecTagBatchJobReply:
            type: object
            properties:
                success:
                    type: boolean
        ExecTagBatchJobRequest:
            type: object
            properties:
                tagId:
                    type: string
                batchId:
                    type: string
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        LockMarketPayOrderReply:
            type: object
            properties:
                orderId:
                    type: string
                deductionPrice:
                    type: number
                    format: double
                tradeOrderStatus:
                    type: integer
                    format: int32
        LockMarketPayOrderRequest:
            type: object
            properties:
                userId:
                    type: string
                teamId:
                    type: string
                activityId:
                    type: string
                goodsId:
                    type: string
                source:
                    type: string
                channel:
                    type: string
                outTradeNo:
                    type: string
        MarketTrialReply:
            type: object
            properties:
                trialResult:
                    $ref: '#/components/schemas/TrialBalanceInfo'
        MarketTrialRequest:
            type: object
            properties:
                userId:
                    type: string
                goodsId:
                    type: string
                source:
                    type: string
                channel:
                    type: string
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        TrialBalanceInfo:
            type: object
            properties:
                goodsId:
                    type: string
                goodsName:
                    type: string
                originalPrice:
                    type: number
                    format: double
                deductionPrice:
                    type: number
                    format: double
                targetCount:
                    type: integer
                    format: int32
                startTime:
                    type: string
                endTime:
                    type: string
                isVisible:
                    type: boolean
                isEnable:
                    type: boolean
        UpdateConfigReply:
            type: object
            properties:
                success:
                    type: boolean
        UpdateConfigRequest:
            type: object
            properties:
                key:
                    type: string
                value:
                    type: string
tags:
    - name: ActivityHTTP
      description: GroupBuyService HTTP API
    - name: DccHTTP
      description: DccService HTTP API
    - name: TagHTTP
      description: TagService HTTP API
    - name: TradeHTTP
      description: TradeService HTTP API
